Извини, такое бывает при копировании из чата.

Вот **чистый, готовый Markdown код**. Скопируй его целиком (нажми кнопку "Copy" в правом верхнем углу блока кода) и вставь в свой файл `README.md`.

Я сохранил профессиональный стиль и добавил бейджи для солидности.

```markdown
# 📂 SmartFileSorter

**SmartFileSorter** — это асинхронный сервис для автоматической оркестрации файлов в реальном времени.

Проект разработан как демонстрация архитектурного паттерна **Producer-Consumer**, разделяя логику мониторинга файловой системы (Watchdog) и обработки файлов (Worker) через потокобезопасную очередь.

---

## 🚀 Основные возможности

* **Real-time Monitoring**: Мгновенная реакция на появление файлов (используется библиотека `watchdog`).
* **Clean Architecture**: Четкое разделение ответственности (SRP):
  * `Coordinator` (Producer) — управление наблюдением и фильтрация событий.
  * `Queue` — асинхронный буфер задач.
  * `Mover` (Consumer) — безопасное перемещение файлов в отдельном потоке.
* **Thread-Safety**: Тяжелые операции I/O вынесены в фоновый поток (Worker), не блокируя основной цикл наблюдения.
* **Smart Config**: Гибкая настройка правил перемещения и списков игнорирования через **YAML**.
* **Conflict Prevention**: Автоматическое переименование дубликатов (`file.jpg` -> `file_1.jpg`).
* **Test Coverage**: Ключевые модули покрыты unit-тестами с использованием `Pytest` и `Mock`.

## 🛠 Технический стек

* **Python 3.10+**
* **Watchdog** (File System Events)
* **Threading & Queue** (Concurrency)
* **PyYAML** (Configuration)
* **Pytest** (Testing framework)

## ⚙️ Установка и запуск

### 1. Клонирование репозитория
```bash
git clone [https://github.com/dk-dev-io/SmartFileSorter.git](https://github.com/dk-dev-io/SmartFileSorter.git)
cd SmartFileSorter

```

### 2. Установка зависимостей

Рекомендуется использовать виртуальное окружение:

```bash
python -m venv venv
source venv/bin/activate  # Для Linux/macOS
# venv\Scripts\activate   # Для Windows

pip install -r requirements.txt

```

### 3. Настройка

Проект использует два конфигурационных файла:

* **`config.yaml`**: Настройка путей (откуда следить -> куда переносить) и расширений файлов.
* **`ignore.yaml`**: Список файлов и расширений, которые сервис должен игнорировать (например, `.tmp`, `.DS_Store`).

### 4. Запуск

```bash
python main.py

```

*После запуска сервис перейдет в режим демона и начнет мониторинг папок, указанных в `config.yaml`.*

## 📂 Структура проекта

| Файл | Описание |
| --- | --- |
| `main.py` | Точка входа. Инициализация `Coordinator`, `Worker` и внедрение зависимостей (DI). |
| `coordinator.py` | Класс-оркестратор. Управляет `Watchdog`-наблюдателем и фильтрует события перед отправкой в очередь. |
| `sorter.py` | Логика маршрутизации: определяет целевую папку на основе расширения файла и конфига. |
| `mover.py` | Отвечает за физическое перемещение файлов и разрешение конфликтов имен. |
| `event_handler.py` | Обработчик низкоуровневых событий файловой системы. |

## 🧪 Тестирование

Проект содержит набор unit-тестов. Для запуска выполните:

```bash
pytest

```

---

*Проект разработан в учебных целях для демонстрации навыков QA Automation & Python Engineering.*

```

### Как обновить на GitHub:

1.  Сохрани этот текст в файл `README.md` в папке проекта.
2.  Выполни в терминале:

```bash
git add README.md
git commit -m "Update README docs"
git push

```